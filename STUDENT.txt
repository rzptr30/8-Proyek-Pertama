BASE_URL=https://story-api.dicoding.dev/v1
DEPLOY_URL=https://rzptr30.github.io/8-Proyek-Pertama/

FEATURES:
- SPA + guarded routes + view transitions
- Map + marker dari API (location=1)
- Add story (online) + offline queue (Outbox) + auto-sync saat online
- Auth (register/login/logout)
- PWA installable + offline shell + dynamic caching (stories, images)
- Push Notification (enable/disable, payload dinamis, action ke detail)
- IndexedDB saved stories (CRUD + search + sort) + halaman #/saved
- Aksesibilitas: label-for untuk input, fokus heading, aria drawer, aria-live status
- Background Sync untuk POST /stories

TESTED_BROWSERS=Chrome 131 (Windows), Edge 131
NOTES=Untuk uji notifikasi, pastikan mode Jangan Ganggu OS dimatikan dan izin notifikasi diizinkan pada origin GitHub Pages.